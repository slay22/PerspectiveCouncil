{
  "name": "perspective-council",
  "version": "0.2.0",
  "description": "Multi-agent code review orchestrator — Claude Code + OpenCode + Pi Agent",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch run src/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "diff": "^7.0.0",
    "ora": "^8.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/diff": "^5.2.1",
    "playwright": "^1.61.1",
    "typescript": "^5.5.0"
  }
}
